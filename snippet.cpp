#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int mdata[10];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    for (int i = 0; i < 10; i++)
    {
        cin >> mdata[i];
    }
}